export default function SettingsPage() {
    return (
        <div className="space-y-8">
            <header>
                <h2 className="text-3xl font-bold bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
                    Settings
                </h2>
                <p className="text-gray-400 mt-2">Configure Gateway parameters (Coming Soon).</p>
            </header>

            <div className="bg-gray-900 rounded-xl p-6 border border-gray-800 text-center py-20">
                <p className="text-gray-500">Settings configuration will be available in the next release.</p>
            </div>
        </div>
    );
}
